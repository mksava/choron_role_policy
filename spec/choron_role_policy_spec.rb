# frozen_string_literal: true

RSpec.describe ChoronRolePolicy do
  it "has a version number" do
    expect(ChoronRolePolicy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
